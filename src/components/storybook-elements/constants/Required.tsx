import * as React from 'react';
import { FunctionComponent } from 'react';
import { Icon, IconName } from '../../05-atoms/icon/Icon';

export const Required: FunctionComponent = () => <Icon name={IconName.CHECK} />;
