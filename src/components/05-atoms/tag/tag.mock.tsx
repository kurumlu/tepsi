import * as React from 'react';
import { FunctionComponent } from 'react';
import { Icon, IconName, IconSize } from '../icon/Icon';

export const MockChildren: FunctionComponent = () => (
  <Icon name={IconName.FEMALE} size={IconSize.MEDIUM} />
);
