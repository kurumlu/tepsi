import { IconProps, IconName, IconColor, IconSize } from './Icon';

export const mockIconProps: IconProps = {
  name: IconName.MENU,
  className: 'sd-a-icon-test',
  color: IconColor.POSITIVE,
  size: IconSize.MAJOR,
};
